namespace Logger.src.loggers.enums
{
    internal class SeverityLevel
    {
        public enum Severity
        {
            Information,
            Error,
            Verbose,
            Warning,
            Critical
        }
    }
}
